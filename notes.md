git clone https://github.com/mattpe/git-intro.git

cd git-intro

git remote remove origin

git remote add origin https://github.com/Kemosab3/exercise1

git branch -M main

git push -u origin main

git add .

git commit -m "added notes.md"

git push